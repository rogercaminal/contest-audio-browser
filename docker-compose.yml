version: "3.9"

services:
  contest-audio-browser:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      CONTESTS_ROOT: /data/contests
      EXPORT_DIR: /data/exports
    volumes:
      - ./data/contests:/data/contests
      - ./data/exports:/data/exports

